module Iris.Workflow:0.0.1-SNAPSHOT {
  import Iris.Classify:0.0.1-SNAPSHOT as C;

  def w = Sources.http<{petalLength: Double, petalWidth : Double, sepalLength : Double, sepalWidth : Double}> { http_path = "iris" } 
          | C.predict 
          | Sinks.log<Text>;
}
