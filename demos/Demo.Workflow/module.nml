module Demo.Workflow:0.0.1-SNAPSHOT

import Demo.NumChars:0.0.1-SNAPSHOT as D
import Demo.DigitString:0.0.1-SNAPSHOT as DigitString

// A sample workflow
def w = Sources.http { http_path = "/demo" } | D.numChars | Sinks.log
def digits = Sources.http { http_path = "/digit_string" } | DigitString.digitString | Sinks.log
